export const getRoutes = (state) => {
  return state.routes;
};

export const getMarkers = (state) => {
  return state.markers;
};

export const getMap = (state) => {
  return state.map;
};

export const getMapToken = (state) => {
  return state.mapToken;
};

export const getMarkerId = (state) => {
  return state.markerIdCursor;
};